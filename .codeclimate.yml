engines:
  rubocop:
    enabled: true
  fixme:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        - ruby
ratings:
  paths:
    - lib/**/*.rb
exclude_paths:
  - spec/**/*
